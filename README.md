So, I started this project with:
* Created GH repository
* Cloned locally
* Ran these commands

```
npm init -y
npm install --save-dev nodemon
npm install express
npm install mysql2
```

* Added the `dev` script to `package.json`
* Ran `npm run dev` to start the server
    
If you've just cloned this repo, just run `npm install`